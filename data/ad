CREATE TABLE public.ad (
    id serial NOT NULL,
    customer integer NOT NULL,
    book integer NOT NULL,
    price integer NOT NULL,
    locality integer NOT NULL
);

INSERT INTO public.ad(customer, book, price, locality)
	VALUES (random()*11086, random()*11382, 250+random()*750, random()*106908);